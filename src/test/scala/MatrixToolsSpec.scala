package com.github.akajuvonen.neuralnet_scala

import org.scalatest._

/** MatrixTools test class. */
class MatrixToolsSpec extends FlatSpec {
  "Matrix tools" should "do stuff" in {
    assert(true)
  }
}
